{
  "name": "root",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean --yes",
    "lint": "lerna run lint",
    "release": "lerna publish --exact --no-git-reset",
    "release:dry-run": "lerna version --exact --no-git-tag-version --no-push",
    "test": "lerna run test",
    "updated": "lerna updated"
  },
  "devDependencies": {
    "@commitlint/cli": "^14.1.0",
    "@commitlint/config-conventional": "^14.1.0",
    "husky": "^7.0.2",
    "lerna": "^4.0.0",
    "prettier": "2.1.2"
  },
  "engines": {
    "node": "^10.12.0 || >=12.0.0"
  },
  "private": true
}
